# Routine registry

Every scheduled Claude Code routine attached to this repository, and a journal of what each firing
did. Maintained by the routines themselves, reviewed by a human.

---

## Active routines

| Routine | ID | Schedule (UTC) | Local | Prompt | Model | Branch |
| --- | --- | --- | --- | --- | --- | --- |
| `daily-loop-research` | `trig_012k5hZBEfTpeyTjTaJ2aGcb` | `0 13 * * *` | 06:00 PT | [`daily-loop-research.v1.md`](prompts/daily-loop-research.v1.md) — **trigger's stored bootstrap prompt still points here; see "Prompt versions" below, v2 exists but the live trigger has not been switched to it** | claude-sonnet-5 | `routine/experiments` |

Created 2026-08-02. Run history: <https://claude.ai/code/routines/trig_012k5hZBEfTpeyTjTaJ2aGcb>

Routines cannot be deleted from here; disable or delete at <https://claude.ai/code/routines>.

### `daily-loop-research`

**Purpose.** Control plane for [spec 012](../specs/012-schema-conditioned-policy-synthesis/spec.md).
Advances the build order while it is incomplete; afterwards proposes one loop/graph change per day,
queues it for the compute plane, and reports what came back.

**Authority.** May propose, implement build-order steps, queue experiments, regenerate the delimited
README block, and open PRs. May **not** edit gate thresholds or τ, touch held-out schemas, rewrite
run history, or report a number it did not read from a committed file. Rationale in
[spec.md §9](../specs/012-schema-conditioned-policy-synthesis/spec.md).

**Cost.** Session tokens only. It makes no OpenAI calls and holds no secrets — the compute plane
(GitHub Actions) does both.

---

## Prompt versions

| Version | Date | Change | Reason |
| --- | --- | --- | --- |
| v2 | 2026-08-05 | Added step 0: check `origin/routine/experiments` and its open PR *before* deciding what to build; treat that branch, not `master`, as the repo's true state. | Two firings (2026-08-03, 2026-08-05) rebuilt an already-done step from a stale `master`-only view before catching it. Both times caught pre-push and discarded, but that's luck, not a guarantee. |
| v1 | 2026-08-02 | Initial | — |

Prompts are never edited in place. The version is recorded in every run log; editing a prompt
without bumping it silently invalidates every comparison across that boundary.

**Note on v2's status:** the file (`daily-loop-research.v2.md`) is written and committed, but the
trigger's own stored bootstrap prompt (`trig_012k5hZBEfTpeyTjTaJ2aGcb`, see the Active routines
table above) still hard-codes a pointer to `daily-loop-research.v1.md` by filename. This routine
firing deliberately did **not** call `update_trigger` to repoint it: that changes unattended
automation's behavior immediately, before a human has reviewed this PR, and the tool's own guidance
is to change a routine's prompt only in service of an explicit human request, not on the routine's
own initiative. A future firing still reads v1 until a human (or an explicit instruction from one)
updates the trigger's stored prompt to reference `v2.md` instead. Flagging this plainly rather than
switching it silently.

---

## Run journal

Newest first. One row per firing, including no-ops and failures — a journal that records only
successes cannot tell the human whether the routine is worth its tokens.

| Date | Type | What happened | Evidence read | PR |
| --- | --- | --- | --- | --- |
| 2026-08-08 | `implement` (cron firing, still bootstrapped from v1) | Repeated the same mistake a **fourth** time, for the same reason as the third: the live trigger still bootstraps from `daily-loop-research.v1.md`, which has no step 0. Read `plan.md` §7 against a fresh `master` checkout alone, concluded step 1 (`TrainingScaffold` + compiler-to-MuJoCo) was the lowest-numbered incomplete step, and built a fourth implementation from scratch (`compiler.py`/`baselines.py`/`baseline_check.py`, 9 tests, one real G1 run log) before checking `origin/routine/experiments`. Caught it the same way as every prior time — `git fetch origin routine/experiments` + `list_pull_requests` showed PR #6 open, now with steps 1/2/3/4/5/6(local) done — and discarded the duplicate (`git stash -u` then `git stash drop`, then `git checkout routine/experiments`) before pushing anything. Flagging plainly, again: v2's step 0 fix is written and committed but the trigger's stored prompt has not been repointed to it; that is a human action, not something to change mid-firing. With the real state established (steps 1–5 done, step 6 locally verified via a real Docker daemon on 2026-08-07 but never executed under real GitHub Actions — confirmed via `actions_list`: 0 prior runs of `loop-research.yml`, any branch, ever), did the actual lowest-numbered incomplete step: closed the local-vs-Actions verification gap on step 6. Pushed a placeholder `experiments/queue/2026-08-08-actions-smoke.yaml` (explicitly not a real experiment; step 7 doesn't exist) to fire the workflow's `push` trigger for real. Result: [run #1](https://github.com/atharva789/autobot/actions/runs/31259538315), conclusion `success` — `preflight` job (id `93108086694`) found the manifest and correctly logged `::warning::OPENAI_API_KEY is not set. Add it with: gh secret set OPENAI_API_KEY`, `ready=false`; the `experiment` job (id `93108105016`) was correctly `skipped`, no compute spent. Read the actual job log via `get_job_logs`, not just the job-level conclusion, to confirm the exact branch of preflight's logic that fired (manifest found → orchestrator.py found → secret check failed), rather than inferring it. Also attempted a `workflow_dispatch` run first (to test the manifest-not-found branch too); it failed outright with `403 Resource not accessible by integration` — this session's GitHub App token cannot dispatch workflows, a permission distinct from the push-based path that worked fine and is the one the design actually relies on (plan.md §2, step "push triggers workflow"). Recorded as a new known issue below rather than worked around. Noted, but did not act on: `orchestrator.py`'s own docstring states it makes no model calls yet (step 7 doesn't exist), so the preflight's `OPENAI_API_KEY` gate is currently blocking a run that wouldn't use the key even if present — flagging this as a real observation, not touching the gate (plan.md §8 states the secret as a required human action "before first compute run" without qualifying which run, so treating it as intentionally conservative rather than a bug to fix unilaterally). Could not independently re-run `tests/test_loop_research_*` — this sandbox has no `mujoco` installed (`ModuleNotFoundError` confirmed by attempting a collection run), unlike the sandboxes of firings 2026-08-03 through 2026-08-07 which evidently did; did not install it or run physics locally, consistent with the control-plane/compute-plane split (spec.md §9) — relying on 2026-08-07's committed 85/85 result rather than re-asserting it. No gate threshold, τ, or rubric criterion touched; no `holdout-*` path read or written; no new Compose service added; `.runs/` history untouched (no new run.json — this action produced an Actions run, not a `loop_research` experiment record). | Real-time: `git log origin/routine/experiments --oneline` (confirmed steps 1–6 already on the branch), `list_pull_requests` (PR #6 open), `actions_list` (0 prior `loop-research.yml` runs, then 1 after push), [Actions run #1](https://github.com/atharva789/autobot/actions/runs/31259538315) + its `preflight` job log (`get_job_logs`), `routines/registry.md` (last 5 rows, this file), `routines/prompts/daily-loop-research.v2.md` (step 0's own text). | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-07 | `implement` (cron firing, still bootstrapped from v1) | Checked `origin/routine/experiments` and PR #6 *before* building anything (this time correctly, on the first attempt) — found steps 1/2/4/5 done and step 6 started (2026-08-06: `orchestrator.py`/`gates.py` exist, but `Dockerfile.sim`/`Dockerfile.orchestrator`/`sim_worker.py` do not, blocked yesterday on "no Docker daemon reachable"). Checked that claim first rather than trusting it: this sandbox's `dockerd` starts and `docker info` succeeds, so today built and **ran** the missing pieces instead of writing them blind. Added `infra/loop-research/Dockerfile.{orchestrator,sim}` (minimal compute-plane-only requirements.txt — verified nothing under `loop_research` imports `torch`/`langchain`/`supabase`; orchestrator.py already imported cleanly with only 5 packages installed) and `sim_worker.py` (a Redis-queue → `run_batch` → MinIO worker for the `sim-worker` Compose service; documented, unproduced job format since step 7 doesn't exist yet to feed it — 9 tests against in-memory fakes, no live Redis/MinIO needed for CI). Building images hit this sandbox's TLS-intercepting proxy (`pip install` inside the container fails cert verification against pypi) — worked around it locally only (installed the sandbox's own CA into a throwaway Dockerfile variant, `git status` confirms nothing about that survived into the committed Dockerfiles, which are the clean production versions) to actually exercise `docker compose up --build --exit-code-from orchestrator` end to end. That surfaced and fixed four real defects no static review would have caught: (1) the compose file's run-log volume mounted to `/out`, but the code writes to `.runs/loop_research` relative to its own cwd (`/app`) — every containerized run would have been silently unrecoverable; (2) `--abort-on-container-failure` alongside `--exit-code-from` does not abort on a **successful** container exit, only `--exit-code-from`'s own implied `--abort-on-container-exit` does — with both flags the stack hung after orchestrator finished until the job's 120-minute CI timeout would have killed it; (3) `sim_worker`'s Redis client raced its own `BRPOP` timeout against redis-py's default `socket_timeout` (also 5s) and crashed with `TimeoutError` on a healthy empty queue; (4) two `sim-worker` replicas (`docker-compose.yml` runs 2) both saw `bucket_exists() == False` and raced `make_bucket()` — the loser crashed on MinIO's `BucketAlreadyOwnedByYou`. Fixed all four, rebuilt, and re-ran the full 5-service stack: **orchestrator exits 0, steps 2/4/5 all PASS, the run.json lands correctly under the host's `.runs/loop_research/` via the corrected mount** — the literal shape of step 6's own gate, produced by actually running it, not asserted. Also fixed `orchestrator.py`'s `git_sha`, which always fell back to `"unknown"` in-container (no `.git` in the image, verified by running it) — now prefers a `GIT_SHA` env var, wired from `github.sha` in the workflow, falling back to the git subprocess only when unset. Extracted `scaffold_from_dict` out of `gates.py` (previously a private duplicate) so `sim_worker.py` reconstructs scaffolds the same way instead of a third copy. This is local Docker verification, not an Actions run — the workflow itself still has not executed, correctly gated on the still-pending `OPENAI_API_KEY` secret (plan.md §8, unchanged, human action). The placeholder key used locally (`sk-local-test-placeholder-not-real`) never left this session and was never a real credential — consistent with plan.md §3's control plane never holding one. 4 new tests for the fixes, 9 for `sim_worker` — 85/85 `loop_research` tests passing (81 prior + 4). No gate threshold, τ, or rubric criterion touched; no `holdout-*` path read (confirmed still just its guard `README.md`, last touched 2026-08-02); no *new* Compose service added (still the five plan.md §4 already justifies). Deleted ~25 throwaway `.runs/loop_research/` directories generated while iterating on the Docker fixes (repeated re-runs of step2/4/5 through the container, never committed) — kept exactly one, the final post-fix run, as evidence. | [`run.json`](.runs/loop_research/2026-08-07T14-00-00Z_step6_compose_smoke/run.json) (`status: completed`, `control.orchestrator.all_passed: true`, `git_sha` correctly set via `GIT_SHA`), `tests/test_loop_research_sim_worker.py` (9 new), `tests/test_loop_research_step6.py` (4 new: `git_sha` env precedence/fallback, MinIO bucket-race handling) — 85/85 `loop_research` tests passing, `.github/workflows/loop-research.yml` + `infra/loop-research/docker-compose.yml` (both modified, diffs are the fixes described above) | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-06 | `implement` (first firing under the live trigger, still bootstrapped from v1) | Repeated the same mistake a **third** time, and only a third time because v2's step 0 (checking `origin/routine/experiments` before building) is written but the live trigger still bootstraps from `daily-loop-research.v1.md`, which does not have it: read `plan.md` §7 against a fresh `master` checkout alone, concluded step 1 was the lowest-numbered incomplete step, and built a full third compiler-to-MuJoCo (`compiler.py`/`expr.py`/`rollout.py`/`baseline.py`, 24 tests, one real trained-baseline run log) before checking `origin/routine/experiments`. Caught it the same way as both prior times — `git fetch` + `list_pull_requests` showed PR #6 open with steps 1/2/3/4/5 already done — and discarded the duplicate (`git checkout -- ...`, `git clean`, `rm -rf` the stray run dir, then `git checkout -B routine/experiments origin/routine/experiments`) before pushing anything. Not fixing the prompt today (that is explicitly step 0's job once a human repoints the trigger, not something to do mid-firing on a tangent) — flagging plainly, again, that the fix already exists and is not yet live. With the real state established (steps 1-5 done, 63/63 `loop_research` tests passing), did the actual lowest-numbered incomplete step: step 6, an `orchestrator.py` entrypoint (`packages/research/loop_research/orchestrator.py`) that re-runs `step2_run`/`step4_run`/`step5_run` in sequence, records pass/fail + wall-clock for each without aborting on an earlier failure, and writes one umbrella `ExperimentRun` (empty `scaffolds`/`gates`, the aggregation lives in `control.orchestrator`) — this satisfies the workflow's own preflight check (`.github/workflows/loop-research.yml` no longer reports "Orchestrator not implemented yet"). Also implemented `gates.py` (`python -m packages.research.loop_research.gates --run-id <id>`), the workflow's post-hoc G1 backfill step for a run whose loop crashed mid-write — idempotent, append-only, skips scaffolds with an unrecognized `schema_id` rather than guessing. Ran the real orchestrator locally: **all three steps PASS in 0.68s**, no model calls, cost $0; step5's tau (0.0278) and D_control_max (0.0000) reproduced exactly, a real consistency check against 2026-08-05's derivation. 9 new tests (`tests/test_loop_research_step6.py`) — 72/72 `loop_research` tests passing. Checked, with real commands, how much of step 6's full gate ("green run [through Actions + Compose], logs committed") that leaves open: `docker` client is present but **no daemon is reachable** in this sandbox (`docker info` returns client info only, consistent with plan.md's stated control-plane architecture); `docker compose config` (with required vars supplied) confirms `infra/loop-research/docker-compose.yml` is syntactically valid; but `Dockerfile.sim`, `Dockerfile.orchestrator`, and `packages/research/loop_research/sim_worker.py` **do not exist**, so `docker compose up --build` would fail regardless of the `OPENAI_API_KEY` secret being set. Two independent, clearly separate blockers remain on the full gate: one human-owned (the secret), one engineering (the Dockerfiles + sim_worker, not attempted today — writing untested container plumbing blind, with no daemon here to verify it against, would be exactly the "quiet failure" this project's gates exist to prevent). No gate threshold, τ, or rubric criterion touched; no `holdout-*` path read or written; no *new* Compose service added (the five already justified in plan.md §4 are unchanged). | [`run.json`](.runs/loop_research/2026-08-06T13-35-51Z_orchestrator_30ef1c/run.json) (orchestrator's own umbrella record — `control.orchestrator.all_passed: true`, `wall_clock_s: 0.68`), [`step2 run.json`](.runs/loop_research/2026-08-06T13-35-51Z_step2_82de1b/run.json), [`step4 run.json`](.runs/loop_research/2026-08-06T13-35-51Z_step4_93922c/run.json), [`step5 run.json`](.runs/loop_research/2026-08-06T13-35-51Z_step5_e3a5ab/run.json) (tau/D_control_max reproduced), `tests/test_loop_research_step6.py` (9 new tests) — 72/72 `loop_research` tests passing (63 prior + 9 new), `.github/workflows/loop-research.yml` and `infra/loop-research/docker-compose.yml` (read, not modified) | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-05 | `implement` (cron firing) | Repeated the exact 2026-08-03 mistake for a moment: read `plan.md` §7 against a fresh `master` checkout alone, concluded step 1 was the lowest-numbered incomplete step, and built a full second compiler-to-MuJoCo (`compiler.py`, a hand-written `dev-a.baseline.json`, 8 tests) before checking `origin/routine/experiments`. Caught it the same way as last time — `git fetch` + `list_pull_requests` showed PR #6 open with steps 1/2/4 already done — and discarded the duplicate (`git reset --hard origin/routine/experiments`) before pushing anything. The 2026-08-03 registry row already states the lesson; repeating it here only because it repeated, not because the lesson changed. With the real state established, did the actual lowest-numbered incomplete step: step 5, the structural diff `D(a, b)` (`structural_diff.py`, exact eval-contract.md formula/coefficients) plus the four G2 permutation classes as real MJCF mutations of `dev-a` (`permutations.py`: `link_scaled`, `limits_altered`, `dof_removed`, `topology_swapped`) and the G2/G3 gates (`structural_gates.py`) built on top. Derived tau from measured data (`tau.py`): **D_control_max = 0.0000**, **D_handwritten_min = 0.0556** (the two frozen baselines), **tau = 0.0278**. Ran `step5_run.py`: the negative control fails all four G2 classes and G3 as required; the `dof_removed` hard check also fired for real (control still cited `joint.j_shoulder.qpos` after that joint was deleted from the mutant). While building the `limits_altered`/`link_scaled` permutations, found and fixed a real bug in `dev-a.xml`: no `<compiler angle="radian"/>`, so MuJoCo's degree default silently shrank every joint range ~57x, freezing the arm — verified the fix against all 34 pre-existing `loop_research` tests before relying on it. Flagged, but did not touch: `D_handwritten_min` is small because the baselines intentionally share term/symbol names, making the derived tau a low bar — a candidate research-day topic once the build order is complete, not something today's implementation day may act on (tau stays derived from data, per the hard limits). 29 new tests; 63/63 `loop_research` tests total. No model calls; cost $0. | [`run.json`](.runs/loop_research/2026-08-05T13-36-04Z_step5_256104/run.json), [`gates.json`](.runs/loop_research/2026-08-05T13-36-04Z_step5_256104/gates.json), `tests/test_loop_research_step5.py` (29 new tests) — 63/63 `loop_research` tests passing (34 prior + 29 new) | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-04 | `implement` (cron firing) | Checked `origin/routine/experiments` and PR #6 *before* building anything (the lesson recorded in the row below) — found steps 1-3 done, step 2 complete for the two dev schemas, step 4 (negative control) the lowest-numbered incomplete step. Implemented `static_scaffold_loop` (`static_scaffold_loop.py`): a fixed scaffold hand-tuned for `dev-a` that ignores `schema_path`/`task_text` entirely. Deliberately did **not** reuse `baselines.py`'s per-schema baseline — that scaffold reuses site names (`grip_center`/`payload_center`/`shelf_target`) shared across `dev-a` and `dev-b` by design (for G2/G3 comparability later), which would have let the control accidentally pass G1 on `dev-b` too. Built the control's fixed scaffold from `dev-a`-only joint/body/sensor names instead (`j_shoulder`, `j_elbow`, `wrist`, `finger_left`, `s_touch_left`) so it fails to resolve at all once the topology changes. Ran it (`step4_run.py`) against both dev schemas: **G1 passes 1.0 on dev-a, fails 0.0 on dev-b** — exactly eval-contract.md's required outcome ("Fail on every schema except dev-a"). Also corroborated at the compiler level: `compile_scaffold` succeeds against `dev-a`, raises `CompilationError` against `dev-b`. G2/G3 need the structural diff `D(a, b)` (step 5, not yet implemented) — did not compute or claim a numeric score for either; instead added a test proving the control returns an object-identical scaffold for different inputs, which makes every term of `D` zero by construction regardless of how `D` is later implemented. No gate threshold, τ, or rubric criterion touched; no `holdout-*` path read or written; no Compose service added. | [`run.json`](.runs/loop_research/2026-08-04T13-29-01Z_step4_423c5e/run.json), [`gates.json`](.runs/loop_research/2026-08-04T13-29-01Z_step4_423c5e/gates.json), `tests/test_loop_research_step4.py` (5 new tests) — 34/34 `loop_research` tests passing (29 prior + 5 new) | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-03 | `implement` (first cron firing) | Started from a fresh `master` checkout per the routine's own instructions, read `plan.md` §7 against `master` alone, and initially rebuilt step 1 (`TrainingScaffold` → MuJoCo compiler) from scratch under different module names (`compiler.py`, `expr.py`, `rollout.py`, `records.py`) — duplicating, with a less mature design, the canonical step-1 work already sitting in **open draft PR #6** on `routine/experiments` (reconciled the day before). Caught this before pushing by checking `origin/routine/experiments` and `list_pull_requests`; discarded the duplicate entirely (`git reset --hard origin/routine/experiments`) rather than pushing a second, conflicting implementation. Lesson for future firings, recorded here rather than fixed silently: check this branch and its open PR *before* deciding what to build — `plan.md`'s "read `.runs/loop_research/` from day N-1" assumes prior work is visible, but unmerged PR work on `routine/experiments` is not visible from `master` alone. With the real state established (steps 1 and 3 done, step 2 partial — `dev-a.xml` existed, `dev-b`/`holdout-*` did not), did the actual lowest-numbered incomplete step: authored `evals/policy_synthesis/dev/dev-b.xml`, a 4-DOF Cartesian gantry with a single-jaw pinch gripper and position (servo) actuators, structurally different from `dev-a`'s 6-DOF serial revolute arm with motor (torque) actuators on all three of spec.md §2's required axes (DOF count, kinematic topology, actuator type — asserted directly in `tests/test_loop_research_step2.py` via `model.nu`, `jnt_type`, and `actuator_biastype`). Authored frozen per-schema baselines for both schemas (`baselines.py`, spec.md §2's "one hand-written scaffold per schema, authored once, frozen") and ran both: **G1 passes at 1.0 on both dev-a and dev-b**; both rollouts complete 8 episodes each with a valid `RolloutBatch` (all episodes ended `timeout` under simple, untuned probe policies — this gate is "baselines run", not "baselines succeed"; no success claim is made). 29/29 `loop_research` tests pass. | [`run.json`](.runs/loop_research/2026-08-03T13-39-19Z_step2_ce8fd4/run.json), `tests/test_loop_research_step2.py` (5 new tests), `tests/test_loop_research_scaffold.py` + `tests/test_loop_research_g1.py` (24, unchanged, still passing) | pushed to [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-03 | `implement` (reconciliation, human-directed, PR #6 continued) | `routine/experiments` (PR #6) and `master` (via PR #7, a separate human-directed session) had each built an incompatible implementation of the same spec-012 core: PR #6's `scaffold.py` used typed frozen dataclasses and a symbol grammar supporting multi-level dotted attribute paths (`body.payload.pos.z`, needed by `mujoco_compiler.py` to pull a runtime scalar out of physics state); master's `records.py` used plain dicts and a G1 resolver (`symbol.partition(".")`) that only understood single-level symbols. Flagged the conflict on PR #6 rather than resolving unilaterally (see PR #6 comment); maintainer chose PR #6's implementation as canonical. Reconciled: kept PR #6's `scaffold.py`/`symbols.py`/`expr.py`/`entity_table.py`/`mujoco_compiler.py`/`rollout.py` (step 1) as-is; ported the real `dev-a.xml` schema (4-DOF arm, from PR #7) forward unchanged; rewrote `g1.py` (step 3, the scored gate — 0.80/100% thresholds unchanged from `contracts/eval-contract.md`) against the canonical entity table and multi-level-safe symbol resolution; added the `GateResult`/`ExperimentRun` records to `scaffold.py`, completing data-model.md's four-record set; ported `smoke.py` (the local Haiku-backed loop entry point) onto the typed dataclasses and updated its prompt to describe the canonical symbol grammar. Left PR #7's first run log (`.runs/loop_research/2026-08-03T01-42-44Z_smoke_19ffde/`) untouched — it used the now-superseded dict-shaped scaffold, kept as historical evidence per the append-only rule on `.runs/`, not as a claim about the current G1 implementation. | `tests/test_loop_research_scaffold.py` (19/19), `tests/test_loop_research_g1.py` (7/7, rewritten against the real `dev-a.xml` schema and the canonical grammar) — 26/26 passing locally. | [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-02 | `implement` | First firing. Build order (plan.md §7) was 0/8 — nothing under `packages/research/loop_research/` existed and `.runs/loop_research/` was empty. Took step 1: `TrainingScaffold`/`RolloutBatch` records, symbol-grammar parser, a whitelisted-AST expression compiler (rejects `__import__`, attribute escapes, comprehensions, lambdas — scaffold expressions are agent-authored, so treated as untrusted input), an entity-table reader that parses schemas via MuJoCo's own parser (no hardcoded joint/body names, per loop-contract.md C2), and the scaffold-to-MuJoCo compiler + rollout runner. Added one hand-written scaffold against a fixture schema (explicitly not one of the four locked dev/holdout schemas — those are step 2) to exercise the "a hand-written scaffold trains" gate: compiles without error, and termination causes track real physics (generous step budget → `success` every episode; starved budget → `timeout` every episode). Did not touch steps 2-8, gate thresholds, τ, or any `holdout-*` path. No compute-plane run exists yet, so no gate/cost numbers are reported. | `tests/test_loop_research_scaffold.py` (19/19 passing, run locally against `packages/research/loop_research/`); `specs/012-schema-conditioned-policy-synthesis/plan.md` §7 for build-order position; `routines/prompts/daily-loop-research.v1.md` for the run's own instructions. | [#6](https://github.com/atharva789/autobot/pull/6) |
| 2026-08-03 | `implement` (manual, human-directed session, not a routine firing) | First local smoke of the loop on a substitute provider: Haiku via the repo's `claude-code` adapter (subscription auth, $0 API spend). One evidence-dense call emitted a 5-term/4-termination scaffold for `dev-a`; G1 passed at 1.0. G1 resolver + records implemented with 6 passing tests, including proof G1 fails fabricated symbols. Superseded 2026-08-03 by the reconciliation row above, which ports this schema and re-implements G1 against the canonical grammar — the run log stands as historical evidence, the code it exercised does not. | `.runs/loop_research/2026-08-03T01-42-44Z_smoke_19ffde/run.json` | merged to `master` via #7 |

### Column meanings

- **Type** — `implement` (advanced a build-order step), `research` (proposed a loop change),
  `no-op` (no new evidence), `failed` (the run errored).
- **Evidence read** — the run ids or files the entry's claims came from. An entry with claims and no
  evidence is a review finding, not a record.

---

## Review checklist

For the human, when reviewing a routine PR:

- [ ] Every number in the README block traces to a committed `run.json`.
- [ ] No gate threshold, τ, or rubric criterion changed.
- [ ] **Gate implementations match `contracts/eval-contract.md` exactly** — diff coefficients, G1
      cutoffs, τ derivation. A softened `D` weakens every future result without touching a
      documented constant, so diff the code against the contract, not against the previous commit.
- [ ] No file under a `holdout-*` path was read or written.
- [ ] The proposed change is one change, not several.
- [ ] The increment states what would refute it.
- [ ] The registry row matches what the diff actually does.

## Local substitute mode (active until OpenAI credits arrive)

No OpenAI credits exist yet (2026-08-03). Standing arrangement, maintainer-directed:

- The loop runs **locally** through `packages/research/local_chat_models.py` with
  `--provider claude-code --model haiku` (subscription auth, zero API spend) or `--provider codex`.
  Entry point: `python -m packages.research.loop_research.smoke`.
- A crontab job (`17 9 * * *`, this machine) runs `scripts/daily_budget_check.sh`: it appends to
  `routines/budget-log.md`, and when OpenAI credits exceed **$500** it drops
  `experiments/credits-ready.flag` and fires a desktop notification with the unlock command
  (`gh secret set OPENAI_API_KEY --repo atharva789/autobot`).
- The `credit_grants` endpoint is session-locked (403 with an API key), so the balance is
  `?` in the log until the user sets `OPENAI_BUDGET_USD` in `.env` from the dashboard figure.
  The key's liveness is verified daily via `/v1/models`.
- The compute plane (Actions workflow) stays parked; its preflight correctly reports not-ready
  until the secret exists.

## Known issues

| Issue | Impact | Status |
| --- | --- | --- |
| Spec 012 lived only on `codex/agentic-robot-evals-poc` | Routine clones the default branch; prompt carries a `git fetch && checkout` fallback. | **Resolved 2026-08-02** — PR #3 merged to master |
| `gh` CLI is not installed in the routine sandbox | No impact — the sandbox has the GitHub MCP server's tools instead (`create_pull_request`, `list_pull_requests`, etc.), which the first cron firing used to find and push to the already-open PR #6. | **Resolved 2026-08-03** — confirmed on first cron firing |
| Model ids unverified against the live API | Both tiers now default to `gpt-4.1-mini` (cheap end-to-end is the project's point; escalation only via manifest override citing a run log). Id still not confirmed to resolve billing-side. | Confirm before first compute run |
| Workflow preflight read the secret inside a `run:` block | Reworked: the presence check is computed in the expression layer (`env: OPENAI_KEY_SET`) so no secret text enters the shell. | **Resolved 2026-08-02** |
| Routine sandbox has no `mujoco` installed | Firings 2026-08-03 through 2026-08-07 evidently had it (they ran the physics-dependent `loop_research` suite locally); 2026-08-08's sandbox did not, and did not install it or run physics, per spec.md §9. Whether the control plane is *meant* to have mujoco available is now ambiguous in practice — plan.md §1 says it should not. | Flagged 2026-08-08, unresolved — worth a human decision on whether local physics verification in the routine sandbox is intentional or a standing architecture drift |
| GitHub MCP token cannot `workflow_dispatch` | `actions_run_trigger` (`run_workflow`) returns `403 Resource not accessible by integration`. No impact on the routine's actual design — the `push`-to-`experiments/queue/**` trigger path works fine and was confirmed live 2026-08-08 (Actions run #1). Only blocks a manual/ad-hoc dispatch, which nothing in the routine's design currently needs. | Flagged 2026-08-08, no action needed unless a future step wants manual dispatch |
| Step 6's preflight gates on `OPENAI_API_KEY` even though `orchestrator.py` currently makes no model calls (step 7 doesn't exist) | Not a bug — plan.md §8 states the secret as a human action "before first compute run" without qualifying which run; treated as an intentional conservative gate, not loosened unilaterally. | Flagged 2026-08-08 for human judgment call, not acted on |
